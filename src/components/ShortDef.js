import React from "react";
//Styling And Animation
import styled from "styled-components";
import { motion } from "framer-motion";

const ShortDef = ({ shortDef }) => {
  return (
    <div>
      <p>{shortDef}</p>
    </div>
  );
};

export default ShortDef;
